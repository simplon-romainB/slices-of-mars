import { SocialGesturesDirective } from './social-gestures.directive';

describe('SocialGesturesDirective', () => {
  it('should create an instance', () => {
    const directive = new SocialGesturesDirective();
    expect(directive).toBeTruthy();
  });
});
